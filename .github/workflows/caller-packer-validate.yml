---
# caller-workflow.yml

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
  pull_request_target:
    types: [opened, reopened, edited, synchronize]
    branches:
      - master
      - main

jobs:
  call-reusable-workflow:
    uses: opendaylight/releng-sandbox/.github/workflows/reusable-workflow-packer-validate.yml@master
    secrets:
      env_vars: |
        CREDENTIAL3=${{ secrets.CREDENTIAL3 }}
        CREDENTIAL4=${{ secrets.CREDENTIAL4 }}
      # CLOUD_PASS=${{ needs.credentials.outputs.CLOUD_PASS }}
      # CLOUDS_ENV_B64=${{ needs.credentials.outputs.CLOUDS_ENV_B64 }}
      # CLOUDS_YAML_B64=${{ needs.credentials.outputs.CLOUDS_YAML_B64 }}
